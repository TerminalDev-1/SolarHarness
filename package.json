{
  "name": "solar-harness",
  "private": true,
  "type": "module",
  "bin": { "solar-harness": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "commander": "^14.0.0",
    "ink": "^6.8.0",
    "react": "^19.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.2",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
